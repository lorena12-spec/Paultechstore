import { redirect } from "next/navigation";
import Link from "next/link";
import { getSession } from "@/lib/auth";
import { db } from "@/lib/prisma";
import { naira } from "@/lib/utils";

export default async function Admin(){const s=await getSession();if(!s||s.role!=="ADMIN")redirect("/login");const [products,orders,users]=await Promise.all([db.product.count(),db.order.count(),db.user.count({where:{role:"CUSTOMER"}})]);const revenue=await db.order.aggregate({where:{status:{not:"CANCELLED"}},_sum:{total:true}});return <section className="container py-12"><div className="flex flex-wrap items-center justify-between gap-4"><div><p className="font-semibold text-blue-600">ADMIN</p><h1 className="text-4xl font-black">PaulTech dashboard</h1><p className="mt-2 text-slate-600">Manage products, review customer orders, and respond to payments.</p></div><Link href="/admin/products" className="rounded-xl bg-blue-600 px-5 py-3 font-bold text-white">Manage products</Link></div><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><Link href="/admin/products" className="rounded-2xl border bg-white p-6 shadow-sm hover:border-blue-400"><p className="text-sm text-slate-500">Products</p><p className="mt-2 text-2xl font-black">{products}</p><p className="mt-3 text-sm font-semibold text-blue-600">Manage products</p></Link><Link href="/admin/orders" className="rounded-2xl border bg-white p-6 shadow-sm hover:border-blue-400"><p className="text-sm text-slate-500">Orders</p><p className="mt-2 text-2xl font-black">{orders}</p><p className="mt-3 text-sm font-semibold text-blue-600">Review payments and orders</p></Link><Link href="/admin/customers" className="rounded-2xl border bg-white p-6 shadow-sm hover:border-blue-400"><p className="text-sm text-slate-500">Customers</p><p className="mt-2 text-2xl font-black">{users}</p><p className="mt-3 text-sm font-semibold text-blue-600">View customer accounts</p></Link><div className="rounded-2xl border bg-white p-6"><p className="text-sm text-slate-500">Revenue</p><p className="mt-2 text-2xl font-black">{naira(revenue._sum.total||0)}</p></div></div></section>}
